# 操作相关

本文档记录本项目（LIBERO）的 Git 相关操作，方便以后查阅。

---

## 当前仓库配置

| 名称 | 地址 | 说明 |
|------|------|------|
| `origin` | `git@github.com:dsfesdf/mylibero.git` | 我自己的仓库（主要使用） |
| `upstream` | `https://github.com/Lifelong-Robot-Learning/LIBERO.git` | 官方原仓库（用于同步更新） |

查看当前配置：

```bash
git remote -v
```

正常输出：

```
origin    git@github.com:dsfesdf/mylibero.git (fetch)
origin    git@github.com:dsfesdf/mylibero.git (push)
upstream  https://github.com/Lifelong-Robot-Learning/LIBERO.git (fetch)
upstream  https://github.com/Lifelong-Robot-Learning/LIBERO.git (push)
```

---

## 一、把官方仓库变成自己的仓库（方案一：改 remote）

> 说明：在原仓库基础上直接推送到自己的仓库，保留历史，方便以后继续同步官方更新。

### 1. 在 GitHub 上新建一个空仓库

- 仓库名例如 `mylibero`
- **不要**勾选 "Initialize this repository with a README"，保持空仓库

### 2. 修改远程地址

```bash
# 把官方 origin 改名为 upstream（保留，方便以后同步官方更新）
git remote rename origin upstream

# 添加自己的仓库作为 origin
git remote add origin https://github.com/dsfesdf/mylibero.git

# 确认
git remote -v
```

### 3. 推送内容到自己的仓库

```bash
# 推送 master 分支
git push -u origin master

# 如果想连其他分支（如 X-embodiment、docs）一起推
git push origin --all

# 如果想连 tag 也推
git push origin --tags
```

### 4. （可选）改用 SSH 地址

```bash
git remote set-url origin git@github.com:dsfesdf/mylibero.git
```

---

## 二、关于官方仓库的其他分支（了解即可）

当前 checkout 的是 `master`，`docs` 和 `X-embodiment` 两个分支存在于仓库中，但工作区里没展开，需要 `git checkout` 才能切换查看。

| 分支 | 内容 | 是否需要 |
|------|------|---------|
| `master` | 主代码（跑实验、训练、下数据集用这个） | 需要 |
| `X-embodiment` | 代码修改：支持不同机器人形态（如地面机械臂 on_the_ground_panda），改的是 `libero/libero/envs/` 下的机器人/任务定义 | 一般不需要 |
| `docs` | Sphinx 文档网站（一堆 md/html/图片），跟训练数据无关 | 一般不需要 |

**结论**：常规实验只推 `master` 即可，仓库干净。以后需要再单独推其他分支。

---

## 三、推送时的认证问题

GitHub 已不允许用账号密码做 Git 操作，必须使用 **Personal Access Token (PAT)** 或 **SSH**。

### 方法 A：用 Token 当密码

1. 打开 https://github.com/settings/tokens → **Generate new token** → classic
2. 勾选 **repo** 权限 → 生成 → 复制 `ghp_xxxx...`
3. 推送时输入：
   - Username：`dsfesdf`
   - Password：**粘贴 token**（不是登录密码）
4. 记住凭据（避免每次输）：
   ```bash
   git config --global credential.helper store
   ```

### 方法 B：用 SSH（推荐，一劳永逸）

```bash
# 生成 key（如果还没有）
ssh-keygen -t ed25519 -C "你的邮箱"

# 查看公钥
cat ~/.ssh/id_ed25519.pub
```

把公钥内容粘贴到 https://github.com/settings/keys → **New SSH key**。

然后使用 SSH 地址：

```bash
git remote set-url origin git@github.com:dsfesdf/mylibero.git
git push -u origin master
```

---

## 四、网络问题（代理）

如果报错 `gnutls_handshake() failed: The TLS connection was non-properly terminated`，通常是代理/梯子问题。

```bash
# 查看是否有代理变量
env | grep -i proxy

# 给 git 单独配置代理（端口按自己梯子来，Clash 常见 7890）
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890

# 取消代理
git config --global --unset http.proxy
git config --global --unset https.proxy

# 测试命令行能否连上 GitHub
curl -v https://github.com 2>&1 | head -30
```

---

## 五、同步官方仓库的更新

```bash
# 拉取官方最新代码
git fetch upstream

# 合并到自己分支（或 git rebase upstream/master）
git merge upstream/master

# 推送到自己的仓库
git push origin master
```

---

## 六、日常操作

```bash
# 查看状态 / 分支 / 历史
git status
git branch -a
git log --oneline -5

# 提交改动
git add .
git commit -m "说明信息"
git push origin master

# 新建并推送分支
git checkout -b 分支名
git push -u origin 分支名
```

---

## 七、VSCode 相关

VSCode 没有独立的"仓库绑定"，它**实时读取当前打开文件夹的 `.git/config`**。

- 只要 VSCode 打开的文件夹 = `~/LIBERO`，且该目录 `git remote -v` 的 `origin` 是自己的仓库，VSCode 就会显示正确。
- 若 VSCode 仍显示旧仓库：`Ctrl+Shift+P` → `Developer: Reload Window` 重载窗口。
- VSCode 里改 remote：`Ctrl+Shift+P` → `Git: Edit Remote` 或 `Git: Add Remote`。

---

## 八、进程管理（训练进程）

```bash
# 查找训练进程（pgrep 不会匹配到 grep 自己，推荐）
pgrep -af 'libero/lifelong/main.py'

# 或过滤掉 grep 自身
ps aux | grep 'libero/lifelong/main.py' | grep -v grep

# 结束进程（PID 用上面查到的真实值）
kill <PID>        # 优雅结束
kill -9 <PID>     # 强制结束
pkill -f 'libero/lifelong/main.py'   # 一条命令杀掉所有匹配进程

# 最靠谱：看 GPU 上有没有自己的进程
nvidia-smi
```

> 注意：`ps aux` 输出中各列的数值（如 VSZ、RSS）不是 PID，别拿来 kill。
> 唯一的一行 `grep --color=auto ...` 是 grep 自身，不代表训练在运行。
