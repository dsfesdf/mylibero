# LIBERO 项目框架与流程文档

> LIBERO（**L**ifelong **R**obot **L**earning **B**enchmark）是一个用于研究**多任务 / 终身机器人学习中的知识迁移**的基准测试平台。它基于 robosuite/MuJoCo 物理仿真，提供程序化生成的操作任务、人类遥操作数据集、模仿学习（BC）策略网络，以及多种终身学习算法。

---

## 一、顶层目录结构（工作区根 = LIBERO 仓库根）

```
LIBERO/
├── libero/                  # Python 包（两个子包：libero + lifelong）
│   ├── libero/              # 核心环境/基准/数据资产（见第二节）
│   └── lifelong/            # 终身学习训练框架（算法+策略+训练脚本，见第三节）
├── configs/                 # Hydra 配置文件（实际位于 libero/configs）
├── benchmark_scripts/       # 下载数据集、渲染任务等辅助脚本
├── scripts/                 # 数据集采集、生成任务、检查数据集等工具脚本
├── notebooks/               # 教学 notebook（快速上手、程序化生成演示）
├── templates/               # 任务/场景模板（problem_class / scene xml）
├── myexperiment/            # 自定义实验目录（当前为空脚手架）
├── images/                  # README 图片
├── smoke_env.py             # 环境冒烟测试脚本
├── setup.py, requirements.txt, requirements-lock.txt, environment.yml
├── learnpath.md / liberolearn.md  # 学习路径文档
└── README.md
```

> ⚠️ 注意：真正的 Python 包路径是 **`libero/libero/`**（外层是仓库，内层是同名包）。安装后使用 `import libero.libero...`。

---

## 二、`libero/libero/`（核心：环境 + 基准 + 数据资产）

| 子文件夹/文件 | 功能 |
|---|---|
| `__init__.py` | 定义路径系统：读取 `~/.libero/config.yaml`，提供 `get_libero_path()`（datasets/bddl_files/init_states/assets 路径）。**首次 import 会交互式生成配置文件。** |
| **`benchmark/`** | 基准任务定义 |
| `benchmark/__init__.py` | `Task` 结构、`Benchmark` 抽象类、`register_benchmark` 注册器；定义 **LIBERO_SPATIAL / OBJECT / GOAL / 90 / 10 / 100** 各大 suite 与任务顺序 `task_orders` |
| `benchmark/libero_suite_task_map.py` | 每个 suite 到具体 bddl 任务名的映射 |
| `benchmark/mu_creation.py` | 程序化生成任务（任务生成流程） |
| **`envs/`** | 仿真环境封装 |
| `envs/env_wrapper.py` | `OffScreenRenderEnv`（离屏渲染环境主类） |
| `envs/venv.py` | `SubprocVectorEnv`/`DummyVectorEnv` 向量化并行环境（eval 用） |
| `envs/bddl_base_domain.py` / `bddl_utils.py` | BDDL 任务规范解析、奖励/终止判断 |
| `envs/arenas/` | 不同场景（kitchen/living_room/study/table/coffee_table/empty）搭建 |
| `envs/objects/` | 可操作物体（articulated/hope/google_scanned/turbosquid/target_zones） |
| `envs/robots/` | 机器人本体（mounted_panda / on_the_ground_panda） |
| `envs/predicates/`, `object_states/`, `regions/` | 谓词判断、物体状态、采样区域 |
| `envs/problems/` | 各场景的任务问题定义（tabletop/kitchen/living_room…） |
| **`bddl_files/`** | 任务的 BDDL（行为域定义语言）描述文件 |
| **`init_files/`** | 固定初始状态（`.init` / `.pruned_init`），benchmark 复现用 |
| **`assets/`** | 3D 模型、纹理等仿真资产 |
| **`utils/`** | 工具库 |
| `utils/dataset_utils.py` | HDF5 数据集信息读取 |
| `utils/download_utils.py` | 从服务器/HuggingFace 下载数据集 |
| `utils/task_generation_utils.py` / `bddl_generation_utils.py` | 程序化任务生成工具 |
| `utils/video_utils.py` | `VideoWriter`：评测时录制视频 |
| `utils/log_utils.py`, `time_utils.py`, `mu_utils.py`, `object_utils.py`, `utils.py` | 日志/计时/MuJoCo/对象通用工具 |

---

## 三、`libero/lifelong/`（终身学习训练框架 — 核心）

这是 **训练流程的主战场**：

```
lifelong/
├── main.py            # ★ 训练入口（Hydra）
├── evaluate.py        # ★ 独立评测入口
├── datasets.py        # 数据加载（基于 robomimic 的 HDF5 → 序列数据集）
├── metric.py          # 评测指标：成功矩阵、损失矩阵、raw_obs→tensor、rollout
├── utils.py           # 工具：seed、存/读模型、task embedding、FLOPs、实验目录
├── init_path.py       # 路径初始化
├── algos/             # ★ 终身学习算法
│   ├── base.py        # Sequential（顺序微调基线，所有算法父类；learn_one_task 主循环）
│   ├── multitask.py   # Multitask（联合多任务上限基线）
│   ├── er.py          # Experience Replay（经验回放）
│   ├── ewc.py         # Elastic Weight Consolidation（弹性权重巩固）
│   ├── packnet.py     # PackNet（参数剪枝分配）
│   ├── agem.py        # Averaged Gradient Episodic Memory
│   └── single_task.py # 单任务
├── models/            # ★ 策略网络
│   ├── base_policy.py     # BasePolicy：数据增强、compute_loss、注册器
│   ├── bc_rnn_policy.py   # BC-RNN 策略
│   ├── bc_transformer_policy.py  # BC-Transformer（ResNet + FiLM + 时序 Transformer）
│   ├── bc_vilt_policy.py  # BC-ViLT（视觉-语言联合 Transformer）
│   ├── policy_head.py     # GMM 动作头 + 损失函数
│   └── modules/
│       ├── rgb_modules.py         # 图像编码器（ResNet18 / PatchEncoder / SpatialSoftmax）
│       ├── language_modules.py    # 语言编码器（Identity / MLP）
│       ├── transformer_modules.py # 时序 Transformer 解码器
│       └── data_augmentation.py   # 颜色/平移增强
```

### 关键文件说明

- **`main.py`** — 训练主脚本。流程：读 Hydra 配置 → 取 benchmark → 为每个任务加载 HDF5 数据集 → 生成 task embedding → 创建实验目录 → 实例化算法 → 顺序训练各任务（或 multitask 一次训练全部）→ 每个任务训练后评测已见任务 → 保存 `result.pt`（Loss/Success 混淆矩阵 + 前向迁移指标）。
- **`evaluate.py`** — 独立评测。加载某个 checkpoint → 在 `SubprocVectorEnv`（20 个并行环境）里 rollout → 计算成功率与损失，可选录视频。
- **`algos/base.py`** — `learn_one_task()` 是核心训练循环：每个 epoch 训练 + 每 `eval_every` 次评测、保存最佳模型、计算 AoC（学习速度）。
- **`metric.py`** — `evaluate_success`（rollout 成功率）、`evaluate_loss`（数据集损失）、`raw_obs_to_tensor_obs`（观测→张量）。

---

## 四、训练 / 测试完整流程框架

### 1. 数据准备（Data）
```
download_libero_datasets.py   →  下载 (LFS/HuggingFace) 到 datasets/
   • 每任务一个 HDF5：<suite>/<task_name>_demo.hdf5 （人类遥操作，含 obs + actions）
   • 对应 init_files/ 中的 .pruned_init 固定初始状态
```

### 2. 数据加载（`datasets.py`）
```
HDF5 →(robomimic SequenceDataset)→ 序列采样(seq_len=10)
     →(SequenceVLDataset)→ 附加 task_emb（语言嵌入）
     观测模态：agentview_rgb + eye_in_hand_rgb + gripper_states + joint_states
```

### 3. 训练（`main.py`）
```
① get_benchmark(name) → 取任务套件（如 LIBERO_10）
② 循环加载每个任务数据集 → 组织为 SequenceVLDataset 列表
③ get_task_embs()（BERT/CLIP/GPT2/RoBERTa 编码 instruction）
④ 实例化算法 safe_device(get_algo_class(algo)(n_tasks, cfg))
⑤ 分支：
   - Multitask  → learn_all_tasks()  一次性联合训练
   - 其他 algo  → 逐任务循环 learn_one_task() + 每个任务后评测已见任务
⑥ 记录 Loss/Success 混淆矩阵 + 前向迁移(L_fwd/S_fwd) → result.pt
```

### 4. 测试 / 评测
```
方式A（在线）：训练中自动每个 eval_every 评测（cfg.eval.eval）
方式B（离线）：evaluate.py --benchmark --task_id --algo --policy --seed --load_task
        → 20 并行环境 rollout，max_steps=600
        → 输出 success_rate 与 loss，保存 .stats / 视频
```

### 5. 核心指标
- **S_conf_mat / L_conf_mat**：成功/损失混淆矩阵（`[训练到任务 k][测试任务 p]`）
- **S_fwd / L_fwd**：前向迁移（学习速度，AUC/AoC）
- 对比"学完所有任务后对旧任务的保持能力" → 衡量**灾难性遗忘**

---

## 五、可选组合（README 官方）

| 维度 | 可选值 |
|---|---|
| BENCHMARK | `LIBERO_SPATIAL, LIBERO_OBJECT, LIBERO_GOAL, LIBERO_90, LIBERO_10` |
| POLICY | `bc_rnn_policy, bc_transformer_policy, bc_vilt_policy` |
| ALGO | `base(Sequential), er, ewc, packnet, multitask` |

训练命令：
```shell
export CUDA_VISIBLE_DEVICES=GPU && export MUJOCO_EGL_DEVICE_ID=GPU
python libero/lifelong/main.py seed=SEED benchmark_name=BENCHMARK policy=POLICY lifelong=ALGO
```

---

## 六、配置文件（`libero/configs/`，Hydra 组织）

```
config.yaml            # 顶层（受保护）
data/default.yaml      # 观测模态、seq_len=10、img 128x128、obs_key_mapping
train/default.yaml     # n_epochs=50、batch=32、loss_scale、优化器/调度器默认
eval/default.yaml      # n_eval=20、eval_every=5、max_steps=600、并行进程
lifelong/*.yaml        # 各算法的超参（base/er/ewc/packnet/multitask/agem）
policy/*.yaml          # 各策略及其子模块（image/language/policy_head/...）
reproduce_experiment/  # 复现实验配置
```

---

## 七、其他脚本与目录

| 路径 | 功能 |
|---|---|
| `benchmark_scripts/download_libero_datasets.py` | ★ 下载演示数据集（含 `--use-huggingface`） |
| `benchmark_scripts/check_task_suites.py` | 检查任务套件完整性 |
| `benchmark_scripts/render_single_task.py` | 渲染单个任务 |
| `benchmark_scripts/shasum_files.py` | 文件校验和生成 |
| `scripts/collect_demonstration.py` | ★ 采集人类遥操作演示（生成数据集） |
| `scripts/create_dataset.py` | 创建 HDF5 数据集 |
| `scripts/libero_100_collect_demonstrations.py` | 批量采集 LIBERO-100 演示 |
| `scripts/create_libero_task_example.py` / `create_template.py` | 创建自定义任务/模板 |
| `scripts/get_dataset_info.py` / `get_affordance_info.py` | 数据集信息/可供性信息 |
| `scripts/check_dataset_integrity.py` | 数据集完整性检查 |
| `notebooks/quick_walkthrough.ipynb` | 环境使用快速上手 |
| `notebooks/procedural_creation_walkthrough.ipynb` | 程序化任务生成演示 |
| `notebooks/quick_guide_algo.ipynb` | 算法使用指南 |
| `notebooks/custom_object_example.ipynb` | 自定义物体示例 |
| `templates/problem_class_template.py` / `scene_template.xml` | 新任务类与场景模板 |
| `smoke_env.py` | 环境快速冒烟测试 |

---

## 八、其他重要补充内容

### 1. 命令行入口示例（Task 检索，来自 README）
```python
from libero.libero import benchmark
from libero.libero.envs import OffScreenRenderEnv

benchmark_dict = benchmark.get_benchmark_dict()
task_suite = benchmark_dict["libero_10"]()
task = task_suite.get_task(0)
# env = OffScreenRenderEnv(bddl_file_name=..., camera_heights=128, camera_widths=128)
# 使用固定初始状态 env.set_init_state(init_states[id]) 保证复现
```
> 奖励是**稀疏奖励**（完成任务才 +1），所以官方主攻**终身模仿学习**。

### 2. 关键技术栈
- **仿真**：robosuite + MuJoCo（需 `MUJOCO_EGL_DEVICE_ID` 做无头渲染）
- **数据格式**：robomimic HDF5（`data/attrs` 存 `problem_info`、`env_args`）
- **配置**：Hydra + OmegaConf + easydict
- **语言嵌入**：HuggingFace（BERT/GPT2/CLIP/RoBERTa），由 `cfg.task_embedding_format` 控制
- **可视化/日志**：Weights & Biases（`cfg.use_wandb`）

### 3. 依赖与安装要点
```shell
conda create -n libero python=3.8.13
pip install -r requirements.txt
pip install torch==1.11.0+cu113 torchvision==0.12.0+cu113 --extra-index-url https://download.pytorch.org/whl/cu113
pip install -e .        # 安装 libero 包
python benchmark_scripts/download_libero_datasets.py   # 下数据
```
- 首次运行 `import libero.libero` 会交互式生成 `~/.libero/config.yaml`（定义数据集存放路径）。
- `requirements-lock.txt` 是锁定版本，复现环境时更可靠。

### 4. `myexperiment/` 目前是**空脚手架**
它包含 `configs/ data_audit/ policies/ results/ scripts/` 几个空目录 + 一个空 `README.md`。建议用途映射：
- `configs/` → 自定义的 Hydra 覆盖配置
- `policies/` → 自定义策略网络
- `scripts/` → 训练/评测封装脚本
- `results/` → 实验输出（混淆矩阵、视频、stats）
- `data_audit/` → 数据集审计记录

> 注意：`main.py` 的 `create_experiment_dir()` 硬编码输出到 `./experiments/`，若要把结果落到 `myexperiment/results/`，需改 `utils.create_experiment_dir` 的 `prefix` 或通过其它配置项覆盖。

### 5. 训练产物文件命名
- 单任务：`task{id}_model.pth`、`task{id}_auc.log`
- 多任务：`multitask_model_ep{epoch}.pth`、`multitask_auc.log`
- 汇总：`result.pt`、`config.json`
- 评测：`*_on{task}.stats`、`*_videos/`

### 6. 两个易混淆点
- **`libero/`（外）vs `libero/libero/`（内）**：前者是仓库根，后者是包的一部分；`configs` 实际位于 `libero/configs`（与 `libero` 包同级）。
- **`lifelong` 是训练框架，`libero` 是环境+基准**：二者在包内并列，`libero.lifelong` 依赖 `libero.libero`。

### 7. 学习路径文档
根目录的 `learnpath.md` 与 `liberolearn.md` 是仓库已有的学习导航文档，建议优先阅读，可与本文档对照。

---

## 九、端到端流程速览图

```
┌─────────────────────── 数据准备 ───────────────────────┐
│ download_libero_datasets.py                            │
│   → datasets/<suite>/<task>_demo.hdf5                  │
│   → init_files/<suite>/<task>.pruned_init              │
└───────────────────────────┬────────────────────────────┘
                            ▼
┌─────────────────────── 数据加载 ───────────────────────┐
│ lifelong/datasets.py                                   │
│   robomimic SequenceDataset → SequenceVLDataset(+emb)  │
└───────────────────────────┬────────────────────────────┘
                            ▼
┌─────────────────────── 训练 ──────────────────────────┐
│ lifelong/main.py                                       │
│   benchmark → task_embs → algo = get_algo_class(...)   │
│   Multitask: learn_all_tasks()                         │
│   Others   : for task in tasks: learn_one_task()       │
│              └─ 每 eval_every 评测 + 保存最佳模型       │
│   → experiments/.../result.pt (混淆矩阵 + 前向迁移)    │
└───────────────────────────┬────────────────────────────┘
                            ▼
┌─────────────────────── 评测 ──────────────────────────┐
│ 在线: main.py 训练中自动评测                            │
│ 离线: lifelong/evaluate.py                             │
│   → SubprocVectorEnv(20) rollout(max_steps=600)        │
│   → success_rate / loss → *.stats / 视频               │
└───────────────────────────────────────────────────────┘
```
